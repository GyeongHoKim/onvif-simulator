// Package imgsvc implements the ONVIF Imaging Service over SOAP/HTTP.
//
// The simulator exposes virtual imaging settings (brightness, contrast,
// sharpness, etc.) that clients can query and modify via the standard
// ONVIF Imaging interface.
package imgsvc
