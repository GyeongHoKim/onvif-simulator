# RTSP endpoints for main / sub streams (forwarded to ONVIF clients as-is).
# Streams must already exist and be reachable at runtime.

MAIN_RTSP_URI=rtsp://localhost:8554/live
SUB_RTSP_URI=rtsp://localhost:8554/live2
