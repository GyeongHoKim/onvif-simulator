package main

// CLI subcommand / flag literals shared across main and tests (goconst).
const (
	cliConfigShow     = "show"
	cliConfigValidate = "validate"
	cliLiteralOn      = "on"
	cliLiteralOff     = "off"
)
